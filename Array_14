/* ARRAY -->
1.Collection of data.
2.Can store derived data types such as pointers , structures.
3.One dimensional array is like list(eg.vectors).
4.Two dimensional array is like table(eg.matrix).
*/
// This line is written by priyansh Is hdhd.
#include <stdio.h>
int main()
{
    /*
    int marks[4];
    for (int i = 0; i < 4; i++)
    {
        printf("enter the value of %d element of the array\n", i);              
        scanf("%d", &marks[i]);
    }*/

    /* OR we can simply add elements and aviod first for loop block*/
    // like this--->
    int marks[4] = {487,
                    4578,
                    65487,
                    28};

    for (int i = 1; i <= 4; i++)
    {
        printf("The value of %d element of the array is : %d\n", i, marks[i]);
    }

    return 0;
}
